@using MatBlazor;
@inherits Base.Draggable<Weaver.Data.Models.TextField>;

<MatTextField Value="@Component.MappedValue" Label="@Component.Label" FullWidth="true" @oninput="OnValueChanged" />

@code {
    private Task OnValueChanged(ChangeEventArgs e)
    {
        Component.MappedValue = (string)e.Value;
        return ComponentChanged.InvokeAsync(Component);
    }
}